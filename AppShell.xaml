<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="CommsManager.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:CommsManager.Views"
    Title="CommsManager">

    <TabBar>
        <ShellContent
        Title="Commissions"
        ContentTemplate="{DataTemplate views:CommissionsPage}"
        Route="CommissionsPage"/>
    
        <ShellContent
            Title="Customers"
            ContentTemplate="{DataTemplate views:CustomersPage}"
            Route="CustomersPage"/>
        
    </TabBar>

</Shell>
